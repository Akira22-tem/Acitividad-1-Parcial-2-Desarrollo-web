# Configuración de la aplicación
spring.application.name=api-clientes

# Puerto del servidor
server.port=8889

# Configuración de MySQL - Base de datos para clientes
spring.datasource.url=jdbc:mysql://localhost:3307/clientes_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=

# JPA y Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Swagger OpenAPI
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.title=Documentación de API de Clientes